{
  "name": "raffle-tracker",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.5.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
