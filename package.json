{
  "name": "raffle-tracker",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
